<%= render 'shared/arrow_scroll' %>

<% if @feed_items.any? %>

  <ol class="microposts" id="top">
    <%= render @feed_items %>
  </ol>

	<div id="infinite-scrolling">
	  <%= will_paginate @feed_items %>
	</div>

	<%= render 'shared/arrow_scroll' %>
<% end %>