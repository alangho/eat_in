$('ol.microposts').prepend("<%= escape_javascript(render @micropost) %>");
$('textarea#micropost_content').val('');

publisher = client.publish('/microposts', {
  message: '<%= j render @micropost %>'
});