<li id="micropost-<%= micropost.id %>">
  <%= link_to gravatar_for(micropost.user, size: 50), micropost.user %>
  <span class="user"><%= link_to micropost.user.username, micropost.user %></span>
  <span class="content">
    <%= micropost.content %>
    <%= image_tag micropost.picture.url if micropost.picture? %>
    </span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(micropost.created_at) %> ago.
    <% if current_user?(micropost.user) %>
      <%= link_to "delete", micropost, method: :delete,
                                       data: { confirm: "You sure?" } %>
    <% end %>
  </span>



<!--
  <div class="btn-group">
    <button type="button" class="btn-xs btn-success dropdown-toggle" id="addComment"
      data-toggle="collapse" data-target="#post-<%= micropost.id %>" aria-expanded="false"><span class="expand">+</span>
      Comment 
    </button>
  </div>
  <div id="post-<%= micropost.id %>" class="collapse">
    <div class="well">
      Input comment text field tag here with submit button
    </div>
  </div>
-->


</li>

  <ol class="comments">
  <% if micropost.comments.any? %>
  <%= render micropost.comments %>
<% end %>
</ol>
